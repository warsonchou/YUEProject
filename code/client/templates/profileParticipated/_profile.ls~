Template.profile.helpers ({
	images: ->
		cover = this.cover
		console.log cover
		return UploadForActivity.findbyid cover

})
Template._profile.helpers ({
	images: ->
		alert 1
		cover = this.cover
		console.log cover
		return UploadForActivity.findbyid cover

})
